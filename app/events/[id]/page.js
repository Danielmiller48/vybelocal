export default function EventDetailPage({ params }) {
  const { id } = params;
  return (
    <div className="min-h-screen p-8">
      <h2>📅 Event {id} Detail (Placeholder)</h2>
      {/* Title, Host, Date, Description, RSVP button */}
    </div>
  );
}